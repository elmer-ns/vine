
use #root::Ivy;

pub fn main(&io: &IO) {
  let value: N32 = 42;
  let result = Ivy::run("invalid ivy", &value);

  match result {
    Ok(()) { io.println("unexpected") }
    Err(_error) {}
  }

  assert (value == 42) else { io.println("unexpected"); }
}
