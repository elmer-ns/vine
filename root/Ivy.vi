
use #root::Ext;

pub type LoadError = String;

pub fn run[T; Ext[T]](source: String, &value: &T) -> Result[(), LoadError] {
  let result: Ext = safe $ivy { ${value} = vi:ext#2[root:ivy:run](${source as Ext} ^ ${~value}) };

  result as Result[(), String]
}
